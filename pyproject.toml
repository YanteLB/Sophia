[project]
name = "locals"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.57.0",
    "openai>=1.0.0",
    "pydantic-settings>=2.0.0",
]

[tool.poetry]
package-mode = false
packages = []
